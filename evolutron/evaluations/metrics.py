# coding=utf-8

import numpy as np
from sklearn.metrics import roc_auc_score, average_precision_score


def roc_auc(y_preds, y_targets, y_mask=None):

    if y_mask:
        y_

    return roc_auc_score(y_true=y_true, y_score=y_score)

